# Repository publication

Published repository: https://github.com/argobres/student_dropout_prediction

The public main branch includes the reproducible pipeline, public dataset attribution, notebooks, final report, presentations, reference results and automated tests. Commits use the author-supplied Git identity.

## Continue development

```bash
git clone https://github.com/argobres/student_dropout_prediction.git
cd student_dropout_prediction
git switch -c describe-your-change
# Make and test a focused change.
git add path/to/changed/files
git commit -m "docs: describe the actual change"
git push -u origin describe-your-change
```

Open a pull request on GitHub. Never commit credentials or personal student records. Generated outputs and binary models remain excluded by .gitignore. The preparation commits describe actual work; they do not reconstruct or backdate historical capstone development.
