# Submitted reference results

These tables and figures were copied from the capstone artifacts supplied by the author. They are historical reference results, not claimed to be newly generated. Fresh runs write to `reports/generated/workflow/` or `reports/generated/notebook/` so comparisons never overwrite the submitted evidence.

The original notebook generated group fairness metrics but did not contain the summary-table construction. The supplied summary CSV is retained as reference evidence; use the explicit supplementary aggregation in `student_dropout.fairness_summary` to regenerate those definitions.
