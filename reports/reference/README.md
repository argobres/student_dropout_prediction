# Reference results

These tables and figures are the capstone artifacts. Fresh runs write to `reports/generated/workflow/` or `reports/generated/notebook/` so comparisons never overwrite the submitted evidence.

